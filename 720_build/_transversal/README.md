# _transversal — Andamiaje transversal (TR-1..TR-4)

> Capacidades que **cruzan todos los flujos** y se construyen mínimas en el Tracer Bullet y se afinan
> por bandas (`D-020`, E4/NC-2/`D-015`). Carril compartido entre bandas (no cuelga de `<banda>`).

## Filas transversales (`D-020`)
| TR | Tema | Tracer Bullet |
|----|------|---------------|
| **TR-1** | Persistencia de estado mínima + git (runtime de instancia `fda-*`) | **activa (mínima)** |
| **TR-2** | Gate humano / aprobación del científico de datos | **activa (mínima)** |
| **TR-3** | A/B/C completo, evaluador calibrado, durabilidad, context resets | **diferida** |
| **TR-4** | Control de cambios (CR), knowledge base | **diferida** |

> El peldaño activo de cada TR por banda lo fija el `slice_contract` de esa banda
> (`703_definition/<banda>/slice_contract.md`). Ver detalle de invariantes deferibles/no deferibles
> en `D-020`.
